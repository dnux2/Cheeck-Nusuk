Objective
Apply all critical fixes, important improvements, and bonus items from the technical review report:
* 5 Critical bugs (wrong AI model, wrong schema type, null crash, missing CORS, slow font loading)
* 4 Important improvements (confirm dialog, pagination, empty string validation, real Leaflet map)
* 3 Bonus items (50+ seed pilgrims, sound notification, helmet security headers)
Tasks
T001: Critical — Fix gpt-5.1 → gpt-4o-mini
* Blocked By: []
* Details:
   * In `server/routes.ts` line 121, change `model: "gpt-5.1"` to `model: "gpt-4o-mini"`
   * Also add empty string validation check before OpenAI call: `if (!text.trim()) return res.status(400).json({message:"Text cannot be empty"})`
   * Acceptance: AI translator works and returns real translations
T002: Critical — Fix pilgrimId serial → integer in schema
* Blocked By: []
* Details:
   * In `shared/schema.ts` line 21, change `pilgrimId: serial("pilgrim_id")` to `pilgrimId: integer("pilgrim_id")` in the `emergencies` table (this is a FK column, not a primary key — safe to change)
   * Run `npm run db:push --force` to sync with database
   * Acceptance: emergencies schema is correct and FK works properly
T003: Critical — Add null check in resolveEmergency
* Blocked By: []
* Details:
   * In `server/storage.ts`, before `return resolved` on line 95, add: `if (!resolved) throw new Error(\`Emergency with id ${id} not found`);`
   * Acceptance: Resolving a non-existent emergency throws a proper error instead of crashing
T004: Critical — Add CORS + Helmet middleware
* Blocked By: []
* Details:
   * Install `cors`, `@types/cors`, and `helmet` packages
   * In `server/index.ts`, import and apply `cors()` and `helmet()` before the request logger
   * Acceptance: No CORS errors on cross-origin requests, security headers are set
T005: Critical — Remove unused fonts from index.html
* Blocked By: []
* Details:
   * In `client/index.html`, remove the large second `<link>` tag (line 10) that loads 20+ fonts
   * Keep only the first link (line 9) with Cairo + Tajawal (Arabic fonts)
   * Add Plus Jakarta Sans to the same link or a separate small import
   * Acceptance: Only 2 font families loaded, page loads faster
T006: Important — Add confirmation dialog for resolve emergency
* Blocked By: [T003]
* Details:
   * In `client/src/pages/emergencies.tsx`, wrap the resolve button with shadcn `AlertDialog`
   * Import from `@/components/ui/alert-dialog`
   * Trigger dialog on button click, confirm message: "هل أنت متأكد من إنهاء هذه الحالة؟" / "Are you sure you want to mark this emergency as resolved?"
   * Only call `resolveEmergency.mutate(em.id)` after user confirms
   * Acceptance: Resolve requires two clicks — original button + confirm button in dialog
T007: Important — Add pagination to GET /api/pilgrims
* Blocked By: []
* Details:
   * In `server/routes.ts`, update the `GET /api/pilgrims` handler to read `limit` and `offset` query params (default: limit=100, offset=0)
   * Update `storage.getPilgrims()` in `server/storage.ts` to accept optional `{limit, offset}` params and use `.limit()` and `.offset()` in the drizzle query
   * Acceptance: `GET /api/pilgrims?limit=20&offset=0` works correctly
T008: Bonus — Expand seed data to 50+ diverse pilgrims
* Blocked By: [T002]
* Details:
   * In `server/routes.ts` `seedDatabase()` function, expand the pilgrim seed array to 50+ entries
   * Cover 8+ nationalities: Saudi, Egyptian, Indonesian, Pakistani, Turkish, Malaysian, Nigerian, Bangladeshi, Moroccan, Indian
   * Distribute `locationLat/Lng` around real Makkah coordinates (21.42±0.03, 39.82±0.04)
   * Mix `permitStatus`: Valid / Expired / None
   * Only runs when DB is empty (existing guard already present)
   * Acceptance: Fresh database shows 50+ pilgrims with diverse nationalities on map
T009: Bonus — Sound notification for new emergencies
* Blocked By: []
* Details:
   * In `client/src/pages/emergencies.tsx`, use Web Audio API to play a short beep sound when the active emergency count increases
   * Use `useRef` to track previous count and `useEffect` to detect change
   * Generate a simple 800Hz beep using `AudioContext` — no external files needed
   * Acceptance: A beep plays when a new emergency arrives (simulated by polling)
T010: Important — Replace Mock Map with Real Leaflet Map
* Blocked By: [T008]
* Details:
   * Remove the existing `MockMap` component entirely
   * Replace with a real `react-leaflet` map (already installed) in `client/src/components/real-map.tsx` (or a new pilgrim-map component used by the dashboard and crowd-management pages)
   * Center on Makkah: `center={[21.4225, 39.8262]}` with `zoom={14}`
   * Use OpenStreetMap tiles: `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png`
   * Render each pilgrim as a `<Marker>` using their real `locationLat` and `locationLng` from the DB
   * 3 marker colors: 🔴 red = `emergencyStatus === true`, 🟡 yellow = `permitStatus === "Expired"`, 🟢 green = normal
   * Each marker has a `<Popup>` showing: name, nationality, campaign group, permit status, and view-profile button
   * Fix the default Leaflet marker icon bug with `L.Icon.Default.mergeOptions({iconRetinaUrl, iconUrl, shadowUrl})`
   * Import `leaflet/dist/leaflet.css` in the component file
   * Map container must have a fixed height (e.g. `h-[500px]`) so Leaflet renders correctly
   * Acceptance: Interactive Makkah map with colored markers; clicking a marker shows pilgrim popup with details هل البلان كذا مناسبه